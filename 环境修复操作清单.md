# 编译环境修复操作清单

> **给谁看**：需要在本机编译 goose 的人。不需要懂 Rust。
> **为什么需要**：本机当前**无法完整编译**这个项目。下面说明原因、影响和修复步骤。
> **验证日期**：2026-09-15（在真实环境实测四种组合，结论见下）

---

## 一、先看结论

**问题有两层，必须都解决才能完整编译：**

| 层 | 问题 | 影响 |
|---|---|---|
| 第 1 层 | 仓库路径含中文（`E:\桌面\智能体\goose`） | 编译工具读不了中文路径，直接报错 |
| 第 2 层 | 本机没装 **C++ 编译工具**（缺 `link.exe`） | 部分依赖（含 V8 引擎）编译不了 |

**只要解决第 2 层，第 1 层会自动消失**（装了 C++ 工具后，配合一个"英文入口"即可）。

---

## 二、实测对照表

我实际跑了四种组合，这是结果：

| 仓库路径 | 编译工具 | 结果 |
|---|---|---|
| 含中文 | MSVC（C++ 工具） | ❌ `dlltool`/`ar` 报"找不到文件" —— 工具链读不了中文路径 |
| 含中文 | GNU | ⚠️ 需要额外装 NASM + 两个英文缓存目录才能绕过，**且默认编译仍失败** |
| **英文** | MSVC（C++ 工具） | ✅ **完全正常**（前提是已装 C++ 工具） |
| 英文 | GNU | ⚠️ 只剩 `v8-goose` 失败（V8 引擎强制要求 MSVC） |

**关键发现：不需要把仓库复制或搬走。** 在英文路径建一个"联接"入口即可，仓库还在原地，两边是同一份文件，不占额外空间。

---

## 三、修复步骤

### 步骤 1：建英文入口（5 秒，无需管理员）

在命令行（PowerShell）执行：

```powershell
cmd /c mklink /J "E:\goose" "E:\桌面\智能体\goose"
```

> 这会在 `E:\goose` 建一个指向原仓库的入口。**不是复制**，不占额外磁盘。
> 之后所有编译命令都在 `E:\goose` 下执行。

**验证是否成功**：

```powershell
Test-Path "E:\goose\Cargo.toml"    # 应输出 True
```

### 步骤 2：装 C++ 编译工具（需要管理员权限，约 15-30 分钟）

> ⚠️ **当前会话不是管理员**，这一步需要找有管理员权限的人操作，或右键"以管理员身份运行"。

```powershell
winget install --id Microsoft.VisualStudio.2022.BuildTools --override "--quiet --wait --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"
```

**这是什么**：微软官方的 C++ 编译工具（VS Build Tools），约 3-6 GB。
**为什么必须要**：Rust 在 Windows 上默认用它来把代码变成可执行文件；V8 引擎（`ui/desktop` 的本地推理功能依赖）也只认它。

装完后**重开一个命令行窗口**让环境变量生效，然后验证：

```powershell
Test-Path "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Tools\MSVC"
```

### 步骤 3：设置两个环境变量（无需管理员，只在当前用户生效）

```powershell
setx RUSTUP_HOME "E:\rustup"
setx CARGO_HOME  "E:\cargo"
```

**为什么**：你的 Windows 用户名是中文（`韩正阳`），Rust 默认把工具链和依赖缓存放在 `C:\Users\韩正阳\...`，同样会踩中文路径的坑。这两个变量把它们指到英文目录。

> 注：`E:\rustup` 和 `E:\cargo` 目录**当前已存在且有内容**（我为了排查问题安装的），所以这一步设完即可直接用，不需要重新下载。

**重开命令行窗口**，然后验证：

```powershell
cd E:\goose
cargo --version    # 应输出 cargo 1.96.1
rustc --version    # 应输出 rustc 1.96.1
```

### 步骤 4：完整编译验证

```powershell
cd E:\goose
cargo check --workspace --all-targets
```

**看到 `Finished` 就是成功了。**

---

## 四、如果暂时装不了 C++ 工具（降级方案）

不想装那 3-6 GB，也要能跑测试的话，用这套（**能跑大部分测试，但跑不了 `ui/desktop` 的本地推理**）：

```powershell
$env:RUSTUP_HOME = "E:\rustup"
$env:CARGO_HOME  = "E:\cargo"
$env:RUSTUP_TOOLCHAIN = "1.96.1-x86_64-pc-windows-gnu"
$env:CARGO_TARGET_DIR = "E:\goose-build\target"
$env:PATH = "E:\nasm\nasm-2.16.03;$env:PATH"
cd E:\goose
cargo check -p goose -p goose-cli --no-default-features --features goose-cli/rustls-tls,goose/rustls-tls --all-targets
```

`--no-default-features` 是必需的：默认特性会拉入 V8，而 V8 只能靠 MSVC。

---

## 五、这套环境是怎么来的（备查）

排查过程中我安装/创建了以下内容，都在英文路径下：

| 位置 | 内容 | 大小 | 必需性 |
|---|---|---|---|
| `E:\rustup` | Rust 工具链（含 1.96.1 msvc 与 gnu） | 4.0 GB | ✅ 必需 |
| `E:\cargo\registry` | 依赖源码缓存 | 2.2 GB | ✅ 必需（否则要重下） |
| `E:\nasm` | NASM 汇编器 | 3 MB | 仅降级方案需要 |
| `E:\goose-build\target` | 编译产物 | 15.9 GB | ✅ 必需（否则重编约 1 小时） |
| `E:\goose` | 英文路径联接（若已按步骤 1 创建） | 0 | ✅ 必需 |

> 排查期间工具链下载曾两次卡死（rust-lang 官方 CDN 被限速），
> 改用中科大镜像 `https://mirrors.ustc.edu.cn/rust-static` 后正常。
> 如需重装工具链：`rustup toolchain install 1.96.1 --profile minimal`

**清理提示**：以上共约 22 GB。若不再需要编译，可整体删除，无副作用。

---

## 六、给团队的一句话

> 本机仓库位于中文路径（`E:\桌面\智能体\goose`），导致 Rust/MSVC 工具链无法编译；
> 且本机缺少 C++ 编译工具（VS Build Tools）。修复方式：装 VS Build Tools 后，
> 通过 `E:\goose` 英文联接入口编译，并把 `RUSTUP_HOME`/`CARGO_HOME` 指向英文目录。
> 详见 `环境修复操作清单.md`。
